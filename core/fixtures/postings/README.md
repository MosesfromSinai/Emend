# Eval posting fixtures

Five real, publicly posted new-grad software engineer job descriptions,
fetched verbatim (with source URL and fetch date) for the eval harness in
`core/tests/test_evals.py`. Kept as plain text for `parse_jd`/`tailor` to
run against — not redistributed, used for internal testing only.
